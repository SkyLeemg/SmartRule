<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!--<external-path/>代表的根目录: Environment.getExternalStorageDirectory()
      path=""代表你可以向其它的应用共享根目录及其子目录下任何一个文件了。
    -->
    <external-path
        name="download"
        path="download_file" />
    <root-path name="root_path" path="."/>

</paths>